<UserControl x:Class="RoverGUI.Views.TelemetryScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:RoverGUI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--Dashboard panel-->
        <Grid>
            <controls:Gauge 
                WarningValue="{Binding Speed}"
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                Style="{StaticResource SpeedGaugeStyle}"/>
        </Grid>

        <!--Navigate panel-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Rectangle x:Name="radarCameraRect"
                       Grid.ColumnSpan="3" Grid.RowSpan="3" 
                       StrokeThickness="10" Margin="30"  Fill="Black"/>
        </Grid>

        <!--Clock panel-->
        <StackPanel 
            Grid.Row="2" 
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            >
            <Ellipse Width="24"
                     Height="24"
                     Fill="#FF00A2FF"
                     />
            <TextBlock Text="15:34" 
                       Margin="20,0"
                       Style="{StaticResource TitleTextStyle}" />

            <TextBlock  Text="10 февр. 2037" 
                       Style="{StaticResource TitleTextStyle}"/>

        </StackPanel>
    </Grid>
</UserControl>
